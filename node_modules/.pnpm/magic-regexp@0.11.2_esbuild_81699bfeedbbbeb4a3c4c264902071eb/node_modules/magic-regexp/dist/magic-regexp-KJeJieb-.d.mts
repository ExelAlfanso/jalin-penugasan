//#region src/core/flags.d.ts
type Flag = 'd' | 'g' | 'i' | 'm' | 's' | 'u' | 'y';
/** Generate indices for substring matches */
declare const withIndices = "d";
/** Case-insensitive search */
declare const caseInsensitive = "i";
/** Global search */
declare const global = "g";
/** Multi-line search */
declare const multiline = "m";
/** Allows `.` to match newline characters */
declare const dotAll = "s";
/** Treat a pattern as a sequence of unicode code points */
declare const unicode = "u";
/** Perform a "sticky" search that matches starting at the current position in the target string */
declare const sticky = "y";
//#endregion
//#region src/core/types/join.d.ts
type Join<T extends string[]> = T extends [infer F extends string, ...infer R extends string[]] ? `${F}${Join<R>}` : '';
type UnionToIntersection<Union> = (Union extends Union ? (a: Union) => any : never) extends ((a: infer I) => any) ? I : never;
type UnionToTuple<Union, Tuple extends any[] = []> = UnionToIntersection<Union extends any ? () => Union : never> extends (() => infer Item) ? UnionToTuple<Exclude<Union, Item>, [...Tuple, Item]> : Tuple;
//#endregion
//#region src/core/wrap.d.ts
/**
 * What an input's value is, structurally. `atom` takes a quantifier directly;
 * `quantified` is an atom that already has one, so it can still be rewritten
 * into a capture group but cannot take another.
 */
type InputKind = 'atom' | 'quantified' | 'other';
/** Whether `Value` is one character, optionally escaped. */
type IsSingleChar<Value extends string> = StripEscapes<Value> extends `${infer Head}${infer Tail}` ? Tail extends '' ? Head extends '' ? false : true : false : false;
/** Appends `Quantifier`, grouping `Value` first unless it is a single atom. */
type Quantified<Value extends string, Kind extends InputKind, Quantifier extends string> = [Kind] extends ['atom'] ? `${Value}${Quantifier}` : `(?:${Value})${Quantifier}`;
//#endregion
//#region src/core/inputs.d.ts
/** The lone input's kind, or an atom when the joined value is one character. */
type JoinedKind<Inputs extends InputSource[], Value extends string> = Inputs extends [{
  readonly [KIND]: infer K extends InputKind;
}] ? K : IsSingleChar<Value> extends true ? 'atom' : 'other';
/** This matches any character in the string provided */
declare const charIn: (<T extends string>(chars: T) => CharInput<Escape<T, "\\" | "^" | "-" | "]">>) & CharInput<"">;
/** This matches any character that is not in the string provided */
declare const charNotIn: (<T extends string>(chars: T) => CharInput<`^${Escape<T, "\\" | "^" | "-" | "]">}`>) & CharInput<"^">;
/**
 * This takes a variable number of inputs and matches any of them
 * @example
 * anyOf('foo', maybe('bar'), 'baz') // => /(?:foo|(?:bar)?|baz)/
 * @argument inputs - arbitrary number of `string` or `Input`, where `string` will be escaped
 */
declare function anyOf<Inputs extends InputSource[]>(...inputs: Inputs): Input<`(?:${JoinValuesWith<Inputs, '|'>})`, MapToGroups<Inputs>, MapToCapturedGroupsArr<Inputs>, 'atom'>;
declare const char: Input<".", never, [], "atom">;
declare const word: Input<"\\b\\w+\\b", never, [], "other">;
declare const wordChar: Input<"\\w", never, [], "atom">;
declare const wordBoundary: Input<"\\b", never, [], "other">;
declare const digit: Input<"\\d", never, [], "atom">;
declare const whitespace: Input<"\\s", never, [], "atom">;
declare const letter: Input<"[a-zA-Z]", never, [], "atom"> & {
  lowercase: Input<"[a-z]", never, [], "atom">;
  uppercase: Input<"[A-Z]", never, [], "atom">;
};
declare const tab: Input<"\\t", never, [], "atom">;
declare const linefeed: Input<"\\n", never, [], "atom">;
declare const carriageReturn: Input<"\\r", never, [], "atom">;
declare const not: {
  word: Input<"\\W+", never, [], "other">;
  wordChar: Input<"\\W", never, [], "atom">;
  wordBoundary: Input<"\\B", never, [], "other">;
  digit: Input<"\\D", never, [], "atom">;
  whitespace: Input<"\\S", never, [], "atom">;
  letter: Input<"[^a-zA-Z]", never, [], "atom"> & {
    lowercase: Input<"[^a-z]", never, [], "atom">;
    uppercase: Input<"[^A-Z]", never, [], "atom">;
  };
  tab: Input<"[^\\t]", never, [], "atom">;
  linefeed: Input<"[^\\n]", never, [], "atom">;
  carriageReturn: Input<"[^\\r]", never, [], "atom">;
};
/**
 * Equivalent to `?` - takes a variable number of inputs and marks them as optional
 * @example
 * maybe('foo', exactly('ba?r')) // => /(?:fooba\?r)?/
 * @argument inputs - arbitrary number of `string` or `Input`, where `string` will be escaped
 */
declare function maybe<Inputs extends InputSource[], Value extends string = JoinValues<Inputs>>(...inputs: Inputs): Input<Quantified<Value, JoinedKind<Inputs, Value>, '?'>, MapToGroups<Inputs>, MapToCapturedGroupsArr<Inputs>, 'quantified'>;
/**
 * This takes a variable number of inputs and concatenate their patterns, and escapes string inputs to match it exactly
 * @example
 * exactly('fo?o', maybe('bar')) // => /fo\?o(?:bar)?/
 * @argument inputs - arbitrary number of `string` or `Input`, where `string` will be escaped
 */
declare function exactly<Inputs extends InputSource[], Value extends string = JoinValues<Inputs>>(...inputs: Inputs): Input<Value, MapToGroups<Inputs>, MapToCapturedGroupsArr<Inputs>, JoinedKind<Inputs, Value>>;
/**
 * Equivalent to `+` - this takes a variable number of inputs and marks them as repeatable, any number of times but at least once
 * @example
 * oneOrMore('foo', maybe('bar')) // => /(?:foo(?:bar)?)+/
 * @argument inputs - arbitrary number of `string` or `Input`, where `string` will be escaped
 */
declare function oneOrMore<Inputs extends InputSource[], Value extends string = JoinValues<Inputs>>(...inputs: Inputs): Input<Quantified<Value, JoinedKind<Inputs, Value>, '+'>, MapToGroups<Inputs>, MapToCapturedGroupsArr<Inputs>, 'quantified'>;
//#endregion
//#region src/core/types/escape.d.ts
type Escape<T extends string, EscapeChar extends string> = T extends `${infer Start}${EscapeChar}${string}` ? Start extends `${string}${EscapeChar}${string}` ? never : T extends `${Start}${infer Char}${infer Rest}` ? Char extends EscapeChar ? `${Start}\\${Char}${Escape<Rest, EscapeChar>}` : never : never : T;
type EscapeChar<T extends string> = Escape<T, '\\' | '^' | '-' | ']'>;
type StripEscapes<T extends string> = T extends `${infer A}\\${infer B}` ? `${A}${B}` : T;
type ExactEscapeChar = '.' | '*' | '+' | '?' | '^' | '$' | '{' | '}' | '(' | ')' | '|' | '[' | ']' | '/';
type GetValue<T> = T extends string ? Escape<T, ExactEscapeChar> : T extends Input<infer R, any, any, InputKind> ? R : never;
//#endregion
//#region src/core/internal.d.ts
declare const KIND: unique symbol;
/**
 * A leading `(?:` only encloses the whole value when that value is a lone atom,
 * so anything else has to be wrapped rather than rewritten.
 */
type NamedGroup<V extends string, K extends InputKind, Name extends string> = 'other' extends K ? `(?<${Name}>${V})` : V extends `(?:${infer S})` ? `(?<${Name}>${S})` : `(?<${Name}>${V})`;
type Grouped<V extends string, K extends InputKind> = 'other' extends K ? `(${V})` : V extends `(?:${infer S})${infer E}` ? `(${S})${E}` : `(${V})`;
type GroupedCapture<V extends string, K extends InputKind> = 'other' extends K ? `(${V})` : V extends `(?:${infer S})${'' | '?' | '+' | '*' | `{${string}}`}` ? `(${S})` : `(${V})`;
type Captures = (string | undefined)[];
interface And<V extends string, G extends string, C extends Captures> {
  <I extends InputSource[], CG extends any[] = MapToCapturedGroupsArr<I>>(...inputs: I): Input<`${V}${JoinValues<I>}`, G | MapToGroups<I>, [...C, ...CG]>;
  /** this adds a new pattern to the current input, with the pattern reference to a named group. */
  referenceTo: <N extends G>(groupName: N) => Input<`${V}\\k<${N}>`, G, C>;
}
interface Or<V extends string, G extends string, C extends Captures> {
  <I extends InputSource[], CG extends any[] = MapToCapturedGroupsArr<I>>(...inputs: I): Input<`(?:${V}|${JoinValues<I>})`, G | MapToGroups<I>, [...C, ...CG], 'atom'>;
}
interface After<V extends string, G extends string, C extends Captures> {
  <I extends InputSource[], CG extends any[] = MapToCapturedGroupsArr<I>>(...inputs: I): Input<`(?<=${JoinValues<I>})${V}`, G | MapToGroups<I>, [...CG, ...C]>;
}
interface Before<V extends string, G extends string, C extends Captures> {
  <I extends InputSource[], CG extends any[] = MapToCapturedGroupsArr<I>>(...inputs: I): Input<`${V}(?=${JoinValues<I>})`, G, [...C, ...CG]>;
}
interface NotAfter<V extends string, G extends string, C extends Captures> {
  <I extends InputSource[], CG extends any[] = MapToUndefinedCapturedGroupsArr<I>>(...inputs: I): Input<`(?<!${JoinValues<I>})${V}`, G, [...CG, ...C]>;
}
interface NotBefore<V extends string, G extends string, C extends Captures> {
  <I extends InputSource[], CG extends any[] = MapToUndefinedCapturedGroupsArr<I>>(...inputs: I): Input<`${V}(?!${JoinValues<I>})`, G, [...C, ...CG]>;
}
interface GroupedFn<V extends string, G extends string, C extends Captures, Kind extends InputKind> {
  (): Input<Grouped<V, Kind>, G, [GroupedCapture<V, Kind>, ...C], [Kind] extends ['quantified'] ? 'quantified' : 'atom'>;
}
interface Optionally<V extends string, G extends string, C extends Captures, Kind extends InputKind> {
  <NV extends string = Quantified<V, Kind, '?'>>(): Input<NV, G, C, 'quantified'>;
}
interface Times<V extends string, G extends string, C extends Captures, Kind extends InputKind> {
  <N extends number, NV extends string = Quantified<V, Kind, `{${N}}`>>(number: N): Input<NV, G, C, 'quantified'>;
  /** specify that the expression can repeat any number of times, _including none_ */
  any: <NV extends string = Quantified<V, Kind, '*'>>() => Input<NV, G, C, 'quantified'>;
  /** specify that the expression must occur at least `N` times */
  atLeast: <N extends number, NV extends string = Quantified<V, Kind, `{${N},}`>>(number: N) => Input<NV, G, C, 'quantified'>;
  /** specify that the expression must occur at most `N` times */
  atMost: <N extends number, NV extends string = Quantified<V, Kind, `{0,${N}}`>>(number: N) => Input<NV, G, C, 'quantified'>;
  /** specify a range of times to repeat the previous pattern */
  between: <Min extends number, Max extends number, NV extends string = Quantified<V, Kind, `{${Min},${Max}}`>>(min: Min, max: Max) => Input<NV, G, C, 'quantified'>;
}
interface At<V extends string, G extends string, C extends Captures> {
  lineStart: () => Input<`^${V}`, G, C>;
  lineEnd: () => Input<`${V}$`, G, C>;
}
interface GroupedAs<V extends string, G extends string, C extends Captures, Kind extends InputKind> {
  <K extends string>(key: K): Input<NamedGroup<V, Kind, K>, G | K, [NamedGroup<V, Kind, K>, ...C], 'atom'>;
}
interface Input<V extends string, G extends string = never, C extends (string | undefined)[] = [], Kind extends InputKind = InputKind> {
  /** @internal */
  readonly [KIND]: Kind;
  /**
   * this  takes a variable number of inputs and adds them as new pattern to the current input, or you can use `and.referenceTo(groupName)` to adds a new pattern referencing to a named group
   * @example
   * exactly('foo').and('bar', maybe('baz')) // => /foobar(?:baz)?/
   * @argument inputs - arbitrary number of `string` or `Input`, where `string` will be escaped
   */
  and: And<V, G, C>;
  /**
   * this takes a variable number of inputs and provides as an alternative to the current input
   * @example
   * exactly('foo').or('bar', maybe('baz')) // => /foo|bar(?:baz)?/
   * @argument inputs - arbitrary number of `string` or `Input`, where `string` will be escaped
   */
  or: Or<V, G, C>;
  /**
   * this takes a variable number of inputs and activate a positive lookbehind. Make sure to check [browser support](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp#browser_compatibility) as not all browsers support lookbehinds (notably Safari)
   * @example
   * exactly('foo').after('bar', maybe('baz')) // => /(?<=bar(?:baz)?)foo/
   * @argument inputs - arbitrary number of `string` or `Input`, where `string` will be escaped
   */
  after: After<V, G, C>;
  /**
   * this takes a variable number of inputs and activate a positive lookahead
   * @example
   * exactly('foo').before('bar', maybe('baz')) // => /foo(?=bar(?:baz)?)/
   * @argument inputs - arbitrary number of `string` or `Input`, where `string` will be escaped
   */
  before: Before<V, G, C>;
  /**
   * these takes a variable number of inputs and activate a negative lookbehind. Make sure to check [browser support](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp#browser_compatibility) as not all browsers support lookbehinds (notably Safari)
   * @example
   * exactly('foo').notAfter('bar', maybe('baz')) // => /(?<!bar(?:baz)?)foo/
   * @argument inputs - arbitrary number of `string` or `Input`, where `string` will be escaped
   */
  notAfter: NotAfter<V, G, C>;
  /**
   * this takes a variable number of inputs and activate a negative lookahead
   * @example
   * exactly('foo').notBefore('bar', maybe('baz')) // => /foo(?!bar(?:baz)?)/
   * @argument inputs - arbitrary number of `string` or `Input`, where `string` will be escaped
   */
  notBefore: NotBefore<V, G, C>;
  /** repeat the previous pattern an exact number of times */
  times: Times<V, G, C, Kind>;
  /** this defines the entire input so far as a named capture group. You will get type safety when using the resulting RegExp with `String.match()`. Alias for `groupedAs` */
  as: GroupedAs<V, G, C, Kind>;
  /** this defines the entire input so far as a named capture group. You will get type safety when using the resulting RegExp with `String.match()` */
  groupedAs: GroupedAs<V, G, C, Kind>;
  /** this capture the entire input so far as an anonymous group */
  grouped: GroupedFn<V, G, C, Kind>;
  /** this allows you to match beginning/ends of lines with `at.lineStart()` and `at.lineEnd()` */
  at: At<V, G, C>;
  /** this allows you to mark the input so far as optional */
  optionally: Optionally<V, G, C, Kind>;
  toString: () => string;
}
interface CharInput<T extends string> extends Input<`[${T}]`, never, [], 'atom'> {
  orChar: (<Or extends string>(chars: Or) => CharInput<`${T}${EscapeChar<Or>}`>) & CharInput<T>;
  from: <From extends string, To extends string>(charFrom: From, charTo: To) => CharInput<`${T}${EscapeChar<From>}-${EscapeChar<To>}`>;
}
//#endregion
//#region src/core/types/sources.d.ts
type InputSource<S extends string = string, T extends string = never> = S | Input<any, T, any, InputKind>;
type GroupsOf<Source> = Source extends Input<any, infer K, any, InputKind> ? K : never;
type MapToGroups<T extends InputSource[]> = T extends string[] ? never : GroupsOf<T[number]>;
type MapToCapturedGroupsArr<Inputs extends any[]> = Inputs extends string[] ? [] : Inputs extends [infer First, ...infer Rest] ? First extends Input<any, any, infer CaptureGroups, InputKind> ? [CaptureGroups] extends [never] ? MapToCapturedGroupsArr<Rest> : [...CaptureGroups, ...MapToCapturedGroupsArr<Rest>] : MapToCapturedGroupsArr<Rest> : [];
/** One `undefined` per capture group, for lookarounds whose captures never match. */
type MapToUndefinedCapturedGroupsArr<Inputs extends any[]> = Inputs extends string[] ? [] : Inputs extends [infer First, ...infer Rest] ? First extends Input<any, any, infer CaptureGroups, InputKind> ? [CaptureGroups] extends [never] ? MapToUndefinedCapturedGroupsArr<Rest> : [...{ [K in keyof CaptureGroups]: undefined; }, ...MapToUndefinedCapturedGroupsArr<Rest>] : MapToUndefinedCapturedGroupsArr<Rest> : [];
type JoinValues<T extends any[]> = T extends [infer First, ...infer Rest] ? `${GetValue<First>}${JoinValues<Rest>}` : '';
type JoinValuesWith<T extends any[], Joiner extends string, Prefix extends string = ''> = T extends [infer First, ...infer Rest] ? `${Prefix}${GetValue<First>}${JoinValuesWith<Rest, Joiner, Joiner>}` : '';
//#endregion
//#region src/core/types/magic-regexp.d.ts
declare const NamedGroupsS: unique symbol;
declare const ValueS: unique symbol;
declare const CapturedGroupsArrS: unique symbol;
declare const FlagsS: unique symbol;
type MagicRegExp<Value extends string, NamedGroups extends string | never = never, CapturedGroupsArr extends (string | undefined)[] = [], Flags extends string | never = never> = RegExp & {
  [NamedGroupsS]: NamedGroups;
  [CapturedGroupsArrS]: CapturedGroupsArr;
  [ValueS]: Value;
  [FlagsS]: Flags;
};
type ExtractGroups<T extends MagicRegExp<string, string, (string | undefined)[], string>> = T extends {
  [NamedGroupsS]: infer V;
} ? V : never;
type StringCapturedBy<S extends string> = string & {
  _capturedBy: S;
};
type MapToStringCapturedBy<Ar extends (string | undefined)[]> = { [K in keyof Ar]: Ar[K] extends string ? StringCapturedBy<Ar[K]> | undefined : undefined; };
type MagicRegExpMatchArray<T extends MagicRegExp<string, string, any[], string>> = Omit<RegExpMatchArray, 'groups'> & {
  groups: Record<ExtractGroups<T>, string | undefined>;
} & {
  [index: number | string | symbol]: never;
} & (T extends {
  [CapturedGroupsArrS]: infer CapturedGroupsArr extends (string | undefined)[];
} ? readonly [string | undefined, ...MapToStringCapturedBy<CapturedGroupsArr>] : {});
//#endregion
export { caseInsensitive as A, whitespace as C, Join as D, wordChar as E, unicode as F, withIndices as I, global as M, multiline as N, UnionToTuple as O, sticky as P, tab as S, wordBoundary as T, letter as _, InputSource as a, not as b, MapToGroups as c, carriageReturn as d, char as f, exactly as g, digit as h, StringCapturedBy as i, dotAll as j, Flag as k, Input as l, charNotIn as m, MagicRegExpMatchArray as n, JoinValues as o, charIn as p, MapToStringCapturedBy as r, MapToCapturedGroupsArr as s, MagicRegExp as t, anyOf as u, linefeed as v, word as w, oneOrMore as x, maybe as y };