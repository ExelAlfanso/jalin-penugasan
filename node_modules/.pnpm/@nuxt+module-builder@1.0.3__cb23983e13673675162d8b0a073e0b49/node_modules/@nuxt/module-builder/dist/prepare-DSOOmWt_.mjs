import { t as __exportAll } from "./rolldown-runtime-D7D4PA-g.mjs";
import { n as sharedArgs, t as resolveCwdArg } from "./_shared-CfHVT2WD.mjs";
import { defineCommand } from "citty";
import { resolve } from "pathe";
//#region src/commands/prepare.ts
var prepare_exports = /* @__PURE__ */ __exportAll({ default: () => prepare_default });
var prepare_default = defineCommand({
	meta: {
		name: "prepare",
		description: "Prepare @nuxt/module-builder environment by writing types and stubs"
	},
	args: { ...sharedArgs },
	async run(context) {
		const { runCommand } = await import("@nuxt/cli");
		const cwd = resolveCwdArg(context.args);
		return runCommand("prepare", [cwd], { overrides: {
			compatibilityDate: "2024-04-03",
			typescript: { builder: "shared" },
			imports: { autoImport: false },
			modules: [resolve(cwd, "./src/module"), function(_options, nuxt) {
				nuxt.hooks.hook("app:templates", (app) => {
					for (const template of app.templates) template.write = true;
				});
			}]
		} });
	}
});
//#endregion
export { prepare_exports as n, prepare_default as t };
