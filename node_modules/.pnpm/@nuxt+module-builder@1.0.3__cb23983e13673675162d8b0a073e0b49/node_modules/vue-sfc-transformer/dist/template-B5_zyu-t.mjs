import { parse } from "@babel/parser";
import { MagicString, extractRuntimeEmits, extractRuntimeProps, inferRuntimeType } from "vue/compiler-sfc";
import { isFnExpressionBrowser, isMemberExpressionBrowser } from "@vue/compiler-core";
//#region src/utils/script-setup.ts
const DEFINE_EMITS = "defineEmits";
const DEFINE_PROPS = "defineProps";
const WITH_DEFAULTS = "withDefaults";
const DEFINE_MODEL = "defineModel";
/**
* Pre-transpile script setup block to remove type syntax and replace it with runtime declarations.
* This function only performs minimal error checking, it means that it will preserve all errors that can be triggered at runtime
*/
async function preTranspileScriptSetup(sfc, id) {
	if (!sfc.scriptSetup) throw new Error("[vue-sfc-transformer] No script setup block found");
	const context = await prepareContext(sfc, id);
	const resultBuilder = new MagicString(sfc.scriptSetup.content);
	for (const node of context.ctx.ast) {
		if (node.type === "ExpressionStatement") {
			const processedTypeSyntax = processDefineProps(node.expression, context) || processDefineEmits(node.expression, context) || processWithDefaults(node.expression, context) || processDefineModel(node.expression, context);
			if (processedTypeSyntax !== void 0) resultBuilder.overwrite(node.start, node.end, processedTypeSyntax);
		}
		if (node.type === "VariableDeclaration" && !node.declare) {
			const total = node.declarations.length;
			for (let i = 0; i < total; i += 1) {
				const decl = node.declarations[i];
				if (!decl.init) continue;
				const processedTypeSyntax = processDefineProps(decl.init, context) || processDefineEmits(decl.init, context) || processWithDefaults(decl.init, context) || processDefineModel(decl.init, context);
				if (processedTypeSyntax !== void 0) resultBuilder.overwrite(decl.init.start, decl.init.end, processedTypeSyntax);
			}
		}
	}
	return {
		...sfc.scriptSetup,
		content: resultBuilder.toString()
	};
}
function processDefineProps(node, context) {
	if (!isCallOf(node, DEFINE_PROPS)) return;
	const propsRuntimeDecl = node.arguments[0];
	if (!node.typeParameters) return;
	if (propsRuntimeDecl) context.ctx.error(`${DEFINE_PROPS}() cannot accept both type and non-type arguments at the same time. Use one or the other.`, node);
	const propsTypeDecl = node.typeParameters.params[0];
	if (!propsTypeDecl) return;
	context.ctx.propsTypeDecl = propsTypeDecl;
	const propsStr = extractRuntimeProps(context.ctx) || "{}";
	return `${DEFINE_PROPS}(${propsStr})`;
}
function processDefineEmits(node, context) {
	if (!isCallOf(node, DEFINE_EMITS)) return;
	if (!node.typeParameters) return;
	if (node.arguments[0]) context.ctx.error(`${DEFINE_EMITS}() cannot accept both type and non-type arguments at the same time. Use one or the other.`, node);
	const emitsTypeDecl = node.typeParameters.params[0];
	if (!emitsTypeDecl) return;
	context.ctx.emitsTypeDecl = emitsTypeDecl;
	const emits = extractRuntimeEmits(context.ctx);
	return `defineEmits([${Array.from(emits, (emit) => `"${emit}"`).join(", ")}])`;
}
function processWithDefaults(node, context) {
	if (!isCallOf(node, WITH_DEFAULTS)) return;
	context.ctx.propsRuntimeDefaults = node.arguments[1];
	const res = processDefineProps(node.arguments[0], context);
	if (!res) context.ctx.error(`${WITH_DEFAULTS}' first argument must be a ${DEFINE_PROPS} call.`, node.arguments[0] || node);
	if (!context.ctx.propsTypeDecl) context.ctx.error(`${WITH_DEFAULTS} can only be used with type-based ${DEFINE_PROPS} declaration.`, node);
	if (!context.ctx.propsRuntimeDefaults) context.ctx.error(`The 2nd argument of ${WITH_DEFAULTS} is required.`, node);
	return res;
}
function processDefineModel(node, context) {
	if (!isCallOf(node, DEFINE_MODEL)) return;
	const [modelNameDecl, modelRuntimeDecl] = getDefineModelRuntimeDecl(node, context);
	const modelTypeDecl = node.typeParameters?.params[0];
	if (!modelTypeDecl) return;
	let model = inferRuntimeType(context.ctx, modelTypeDecl);
	let skipCheck = false;
	const hasBoolean = model.includes("Boolean");
	const hasFunction = model.includes("Function");
	if (model.includes("Unknown")) if (hasBoolean || hasFunction) {
		skipCheck = true;
		model = model.filter((t) => t !== "Unknown");
	} else model = ["null"];
	if (!model || model.length === 0) return;
	const modelCodegenDecl = {
		type: "ObjectExpression",
		properties: [{
			type: "ObjectProperty",
			key: {
				type: "StringLiteral",
				value: "type"
			},
			value: model.length === 1 ? {
				type: "Identifier",
				name: model[0]
			} : {
				type: "ArrayExpression",
				elements: model.map((name) => ({
					type: "Identifier",
					name
				}))
			},
			computed: false,
			shorthand: false
		}]
	};
	if (modelRuntimeDecl) modelCodegenDecl.properties.push({
		type: "SpreadElement",
		argument: modelRuntimeDecl
	});
	const codegenArgs = [];
	if (modelNameDecl) codegenArgs.push(`"${modelNameDecl.value}"`);
	const codegenType = model.length === 1 ? model[0] : `[${model.join(", ")}]`;
	const codegenSkipCheck = skipCheck ? "skipCheck: true" : "";
	const codegenExtra = modelRuntimeDecl ? `...${context.ctx.getString(modelRuntimeDecl)}` : "";
	codegenArgs.push(`{ ${[
		`type: ${codegenType}`,
		codegenSkipCheck,
		codegenExtra
	].filter((s) => !!s).join(", ")} }`);
	return `${DEFINE_MODEL}(${codegenArgs.join(", ")})`;
}
function getDefineModelRuntimeDecl(node, context) {
	const [arg0, arg1] = node.arguments;
	if (arg0 && arg0.type === "StringLiteral") {
		if (arg1 && arg1.type !== "ObjectExpression") context.ctx.error(`${DEFINE_MODEL}()'s second argument must be an object.`, arg1);
		return [arg0, arg1];
	}
	if (arg0 && arg0.type !== "ObjectExpression" && !(arg0.type === "Identifier" && arg0.name === "undefined")) context.ctx.error(`Unexpected argument type for ${DEFINE_MODEL}().`, arg0);
	return [void 0, arg0];
}
async function prepareContext({ script, scriptSetup }, id) {
	const helper = /* @__PURE__ */ new Set();
	const ast = parse(`${scriptSetup.content}\n${script?.content}`, {
		sourceType: "module",
		plugins: ["tsx", "jsx"].includes(scriptSetup.lang) ? ["typescript", "jsx"] : ["typescript"]
	});
	return { ctx: {
		filename: id,
		source: scriptSetup.content,
		ast: ast.program.body,
		warn: (msg) => {
			console.warn(`[vue-sfc-transformer] ${msg}`);
		},
		error: (msg) => {
			throw new Error(`[vue-sfc-transformer] ${msg}`);
		},
		helper: (key) => {
			helper.add(key);
			return `_${key}`;
		},
		getString: (node) => {
			return scriptSetup.content.slice(node.start, node.end);
		},
		propsTypeDecl: void 0,
		propsRuntimeDefaults: void 0,
		propsDestructuredBindings: Object.create(null),
		emitsTypeDecl: void 0,
		isCE: false,
		options: {}
	} };
}
function isCallOf(node, test) {
	return !!(node && node.type === "CallExpression" && node.callee.type === "Identifier" && (typeof test === "string" ? node.callee.name === test : test(node.callee.name)));
}
//#endregion
//#region src/utils/template.ts
function handleNode(node, addExpression, track) {
	if (!node) return;
	const currentTrack = [...track, node];
	const search = (node) => handleNode(node, addExpression, currentTrack);
	switch (node.type) {
		case 0:
			for (const child of node.children) search(child);
			return;
		case 1: {
			const nodes = [...node.children, ...node.props];
			for (const child of nodes) search(child);
			return;
		}
		case 2: return;
		case 3: return;
		case 4:
			if (node.ast === null || node.ast === false) return;
			addExpression({
				loc: node.loc,
				src: node.content,
				track: currentTrack
			});
			return;
		case 5:
			search(node.content);
			return;
		case 6:
			search(node.value);
			return;
		case 7: {
			const nodes = [...node.forParseResult ? [
				node.forParseResult?.source,
				node.forParseResult?.value,
				node.forParseResult?.key,
				node.forParseResult?.index
			] : [node.exp]].filter((item) => !!item);
			for (const child of nodes) search(child);
			return;
		}
		case 8:
			if (!node.ast) return;
			addExpression({
				loc: node.loc,
				src: node.loc.source,
				track: currentTrack
			});
			return;
		default: throw new Error(`Unexpected node type: ${node.type}`);
	}
}
async function transpileVueTemplate(content, root, offset = 0, transform) {
	const expressions = [];
	handleNode(root, (...items) => expressions.push(...items), []);
	if (expressions.length === 0) return content;
	const s = new MagicString(content);
	const transformMap = await transformJsSnippets(expressions, transform);
	for (const item of expressions) {
		item.replacement = transformMap.get(item) ?? item.src;
		if (!shouldReplaceQuote(item)) continue;
		const sourceQuote = getSourceQuote(content, item.loc.start.offset - offset, item.loc.end.offset - offset);
		if (sourceQuote !== null) {
			const search = sourceQuote === `"` ? `'` : `"`;
			item.replacement = replaceQuote(item.replacement, search, sourceQuote);
		}
	}
	for (const item of expressions) if (item.replacement && item.replacement !== item.src) s.overwrite(item.loc.start.offset - offset, item.loc.end.offset - offset, item.replacement);
	return s.toString();
}
function replaceQuote(code, target, replace) {
	let res = code;
	if (res.includes(target)) {
		/**
		* Due to the way Vue parses templates,
		* the symbol of target would never appear in the code.
		* We just need to replace the symbol of target.
		*
		* But for replace symbol exist in code, we need to escape it,
		* because esbuild have removed the escape character.
		*/
		res = res.replaceAll(replace, `\\${replace}`);
		res = res.replaceAll(target, replace);
	}
	return res;
}
function shouldReplaceQuote(expression) {
	if (!expression.src.includes(`'`) && !expression.src.includes(`"`)) return false;
	if (expression.track.at(-2)?.type === 5) return false;
	return true;
}
function getSourceQuote(code, start, end) {
	const source = code.slice(start, end);
	for (const quote of ["\"", "'"]) if (source.includes(quote)) return quote;
	return null;
}
const WRAPPER_REGEX = /^(wrapper_\d+)\(([\s\S]*?)\);$/;
const WRAPPER_ARROW_REGEX = /^(wrapper_\d+)\(\(\) => \{([\s\S]*?)\}\);$/;
const TRAILING_SEMICOLON_REGEX = /;$/;
const CONST_WRAPPER_REGEX = /^const([\s\S]*?)=\s+wrapper_\d+\(\);$/;
const snippetHandlers = [
	{
		key: (node) => {
			const key = `destructure$:${node.src}`;
			const lastTrack = node.track.at(-1);
			const secondLastTrack = node.track.at(-2);
			if (secondLastTrack?.type === 7 && secondLastTrack.name === "slot") return key;
			if (secondLastTrack?.type === 7 && secondLastTrack.name === "for" && secondLastTrack?.forParseResult && lastTrack !== secondLastTrack.forParseResult.source) return key;
			return null;
		},
		prepare: (node, id) => `const ${node.src} = wrapper_${id}();`,
		parse: (code) => {
			const [_, res] = code.match(CONST_WRAPPER_REGEX) ?? [];
			if (!res) return;
			return res.trim();
		},
		standalone: true
	},
	{
		key: (node) => {
			const key = `multipleStatements$:${node.src}`;
			const secondLastTrack = node.track.at(-2);
			const lastTrack = node.track.at(-1);
			if (lastTrack?.type === 4 && secondLastTrack?.type === 7 && secondLastTrack.name === "on") {
				const isMemberExp = isMemberExpressionBrowser(lastTrack);
				const isInlineStatement = !(isMemberExp || isFnExpressionBrowser(lastTrack));
				const hasMultipleStatements = node.src.includes(";");
				if ((isInlineStatement || isMemberExp) && hasMultipleStatements) return key;
			}
			return null;
		},
		prepare: (node, id) => `wrapper_${id}(() => {${node.src}});`,
		parse: (code) => {
			const [_, wrapperName, res] = code.trim().match(WRAPPER_ARROW_REGEX) ?? [];
			if (!wrapperName || !res) return;
			return res.trim().replace(TRAILING_SEMICOLON_REGEX, "");
		},
		standalone: false
	},
	{
		key: (node) => `default$:${node.src}`,
		prepare: (node, id) => `wrapper_${id}(${node.src});`,
		parse: (code) => {
			const [_, wrapperName, res] = code.match(WRAPPER_REGEX) ?? [];
			if (!wrapperName || !res) return;
			return res;
		},
		standalone: false
	}
];
function getKey(expression) {
	for (const handler of snippetHandlers) {
		const key = handler.key(expression);
		if (key) return {
			key,
			handler
		};
	}
}
function generateSnippetSplitter() {
	const identify = Math.random().toString(36).substring(2, 15);
	return `\nsplitter(${JSON.stringify(identify)});\n`;
}
async function transformJsSnippets(expressions, transform) {
	const transformMap = /* @__PURE__ */ new Map();
	let id = 0;
	for (const expression of expressions) {
		const res = getKey(expression);
		if (!res) continue;
		if (transformMap.has(res.key)) {
			transformMap.get(res.key).nodes.push(expression);
			continue;
		}
		transformMap.set(res.key, {
			id,
			nodes: [expression],
			handler: res.handler
		});
		id += 1;
	}
	const resultMap = /* @__PURE__ */ new Map();
	const orders = [...transformMap.values()];
	const batch = orders.filter(({ handler }) => !handler.standalone);
	const standalone = orders.filter(({ handler }) => handler.standalone);
	try {
		await Promise.all([transformSnippetBatch(batch, false), transformSnippetBatch(standalone, true)]);
		async function transformSnippetBatch(items, scoped) {
			if (items.length === 0) return;
			const batchInputSplitter = generateSnippetSplitter();
			const lines = (await transform(items.map(({ id, nodes, handler }) => {
				const prepared = handler.prepare(nodes[0], id);
				return scoped ? `{\n${prepared}\n}` : prepared;
			}).join(batchInputSplitter))).split(batchInputSplitter).map((l) => l.trim()).filter((l) => !!l);
			if (lines.length !== items.length) throw new Error("[vue-sfc-transform] Syntax Error");
			for (let i = 0; i < items.length; i++) {
				let line = lines[i];
				const { id, handler, nodes } = items[i];
				if (scoped) line = line.startsWith("{") && line.endsWith("}") ? line.slice(1, -1).trim() : "";
				const res = handler.parse(line, id);
				if (!res) continue;
				for (const node of nodes) resultMap.set(node, res);
			}
		}
	} catch (error) {
		throw new Error("[vue-sfc-transform] Error parsing TypeScript expression in template", { cause: error });
	}
	return resultMap;
}
//#endregion
export { preTranspileScriptSetup as n, transpileVueTemplate as t };
